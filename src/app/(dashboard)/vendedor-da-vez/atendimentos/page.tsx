'use client'
import { PlaceholderPage } from '@/components/layout/PlaceholderPage'
import { ClipboardList } from 'lucide-react'
export default function Page() {
  return <PlaceholderPage icon={ClipboardList} title="Atendimentos" description="Histórico de atendimentos presenciais: chamada, aceite, início, finalização e resultado (venda, troca, retorno agendado, sem interesse, etc.). Tudo auditado. Estrutura preparada para fase futura." />
}
