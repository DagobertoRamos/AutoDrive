// =============================================================================
// Re-export de compatibilidade — use notificationStore.ts diretamente
// =============================================================================
export { useNotificationStore } from './notificationStore'
export {
  selectNotifications,
  selectToasts,
  selectUnreadCount,
  selectIsFetching,
  selectIsCenterOpen,
} from './notificationStore'
